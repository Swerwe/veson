import React from 'react';
import classes from "./Collection.module.css"

interface IProps {
    className: string;
}

const Collection = () => {
    return (
        <div>
            Collection
        </div>
    );
};

export default Collection;